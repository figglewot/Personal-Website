@{
    ViewBag.Title = "Resume";
}

<div class="row">
    <div class="col-md-2" id="resumeSkillSection">
        @{Html.RenderAction("Skills");}
        @{Html.RenderAction("Interests");}
        @{Html.RenderAction("Certifications");}
    </div>
    <div class="col-md-8" id="resumeExperienceSection">
        @{Html.RenderAction("Experience");}
    </div>
</div>

